#include <stdio.h>
/**
 * main - Here we go we are asked to finally use printf
 * Return: 0 (complete)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
